<%= content_tag "page", "Loading...", id: "page", :class => "container", data: { 
  team_id: @team_id,
  task_id: @task_id,
  teams: @teams,
  tasks: @tasks,
  vacancies: @vacancies,
  comments: @comments,
  user: @user
} %>